all:
  vars:
    ansible_python_interpreter: "/usr/bin/python3"
  children:

    prox:
      children:

        vms:
          hosts:
            media-box:
              prox_id: "102"
            dev1:
              prox_id: "103"
            dev2:
              prox_id: "104"

        containers:         # lxc containers
          hosts:
            jellyfin-lxc:
              prox_id: "108"

    physical:
      hosts:
        prox-server:
        pi:
          ansible_connection: local
          arch: arm
 
    terminal_users:
      hosts:
        pi:
      children:
        vms:
