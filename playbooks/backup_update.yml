---
- hosts: prox-server
  gather_facts: false
  roles:
    - role: start_prox_server

- hosts: prox
  gather_facts: false   # some hosts might not be started until after this
  roles:
    - role: start_prox

- hosts: "prox,pi"
  become: true
  roles:
    - role: mount_omv
    - role: backup


# TODO:
      # backup
      # update
